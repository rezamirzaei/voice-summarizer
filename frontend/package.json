{
  "name": "voice-summarize-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "lint": "eslint \"src/app/**/*.js\"",
    "build": "rm -rf dist && mkdir -p dist/vendor && cp -R src/* dist/ && cp node_modules/angular/angular.min.js dist/vendor/angular.min.js",
    "dev": "http-server src -p 8080 -c-1"
  },
  "dependencies": {
    "angular": "1.8.3"
  },
  "devDependencies": {
    "eslint": "8.57.1",
    "eslint-config-prettier": "9.1.0",
    "http-server": "14.1.1"
  }
}
